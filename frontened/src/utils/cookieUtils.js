/**
 * No storage utilities needed - backend handles everything via cookies
 * Frontend gets user data from API calls
 */

// This file is kept minimal - all auth is handled by backend cookies
export const placeholder = true;